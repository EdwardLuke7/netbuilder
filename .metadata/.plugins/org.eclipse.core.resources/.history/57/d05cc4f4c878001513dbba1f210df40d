import java.sql.ResultSet;

public class JDBCDAO implements ProductDAO {
	public Product fetch() {
		ResultSet result = JDBCDatabaseManager.query("");
		
		Product product = new Product(0, null, 0, false, null);
		
		return product;
	}
}
